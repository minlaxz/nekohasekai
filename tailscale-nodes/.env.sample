# headscale pre-auth key, single-use, non-ephemeral. Only used on first registration; state in /data/tailscale.
# headscale preauthkeys create --user <user-id> --expiration 1h
TSN_AUTH_KEY=
TSN_CONTROL_URL=https://headscale.example.com
TSN_HOSTNAME=vllm
