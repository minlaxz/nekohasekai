# headscale pre-auth key, reusable + ephemeral. Keep it set: the node registers anew on every start.
# headscale preauthkeys create --user <user> --reusable --ephemeral --expiration 1y
TSN_AUTH_KEY=
TSN_CONTROL_URL=https://headscale.example.com
TSN_HOSTNAME=vllm
